<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= filme.titulo %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <div class="mt-4">
            <h1 class="text-center mb-4"><%= filme.titulo %></h1>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <img src="data:image/jpeg;base64,<%= filme.imagem.toString('base64') %>" class="img-fluid rounded" alt="<%= filme.titulo %>" width="500px" height="650px">
                </div>
                <div class="col-md-6">
                    <div class="p-4 rounded border shadow-sm bg-light">
                        <p><strong>Descrição:</strong> <%= filme.descricao %></p>
                        <p><strong>Ano de Lançamento:</strong> <%= filme.ano_lancamento %></p>
                        <p><strong>Categoria:</strong> <%= categorias.find(c => c.id == filme.id_categoria).nome %></p>
                        <a href="/index" class="btn btn-secondary">Voltar</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>
